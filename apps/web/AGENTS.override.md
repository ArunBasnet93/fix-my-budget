# Web app overrides (apps/web)

## Stack
- Next.js (App Router) + TypeScript
- Tailwind CSS

## Files to touch for landing page
- `src/app/page.tsx` (landing page)
- Keep styling in Tailwind classes (no extra CSS unless asked).

## Run checks
- `npm run lint` before finishing.
